# EscapeRoomTimer 프로젝트 작업 지침

## 3. 작업 원칙

다음 원칙을 반드시 지킨다.

- 기존 프로젝트 구조를 최대한 유지한다.
- 최소한의 코드 변경으로 문제를 해결한다.
- 불필요한 리팩터링을 하지 않는다.
- 파일명, 클래스명, 함수명, 패키지 구조를 임의로 변경하지 않는다.
- 이번 작업과 관계없는 코드를 정리하거나 개선하지 않는다.
- 새로운 라이브러리는 꼭 필요한 경우가 아니면 추가하지 않는다.
- 기존 기능의 동작 방식과 UI를 임의로 변경하지 않는다.
- 실제 매장에서 장시간 실행된다는 점을 고려한다.
- 임시방편보다 재현 가능하고 안정적인 해결 방법을 사용한다.
- 사용자의 명시적인 요청 없이 커밋, 푸시, 브랜치 생성 또는 대규모 파일 삭제를 하지 않는다.

## 4. 분석 범위 제한

처음부터 프로젝트 전체를 분석하지 않는다.

우선 다음 순서로 작업한다.

1. 이번 문제와 직접 관련된 파일만 찾는다.
2. 관련된 호출 흐름만 추적한다.
3. 원인을 확인하는 데 필요한 파일만 추가로 확인한다.
4. 관련 없는 모듈과 파일은 열거나 분석하지 않는다.

검색 결과가 많더라도 모든 파일을 읽지 말고, 실제 호출 관계가 있는 파일만 확인한다.

추측만으로 코드를 수정하지 않는다. 로그, 호출 흐름, 상태 변경 과정 또는 데이터 전달 경로를 통해 원인을 먼저 확인한다.

## 5. 코드 수정 절차

다음 순서대로 작업한다.

### 1단계: 현재 상태 확인

- 현재 Git 변경 사항을 확인한다.
- 기존에 사용자가 수정한 내용을 덮어쓰지 않는다.
- 작업 시작 전부터 존재하던 변경 사항과 이번 변경 사항을 구분한다.

### 2단계: 원인 분석

- 문제 재현 경로를 확인한다.
- 관련 이벤트, 상태, 네트워크 요청 또는 함수 호출 흐름을 추적한다.
- 가장 가능성이 높은 원인을 짧게 정리한다.

### 3단계: 최소 수정

- 원인을 해결하는 데 필요한 코드만 수정한다.
- 가능하면 기존 함수와 구조를 재사용한다.
- 동일한 목적의 새로운 시스템을 중복으로 만들지 않는다.

### 4단계: 정적 검증

수정 후 다음 항목을 우선 확인한다.

- 컴파일 오류
- 잘못된 import
- 타입 오류
- null 처리
- 코루틴 생명주기
- 상태 중복 변경
- 네트워크 연결 해제 처리
- 메모리 누수 가능성
- 기존 기능에 미치는 영향

## 6. 빌드 및 테스트 지침

테스트는 사용량과 시간을 아끼기 위해 단계적으로 수행한다.

우선 수행할 테스트:

- 수정된 모듈만 컴파일한다.
- 가능한 경우 관련 단위 테스트만 실행한다.
- 수정된 앱의 Debug 빌드만 확인한다.
- 실패하면 전체 빌드 전에 오류 원인을 먼저 분석한다.

처음부터 다음 작업을 수행하지 않는다.

- 전체 프로젝트 반복 빌드
- 관련 없는 테스트 전체 실행
- 여러 에뮬레이터 동시 실행
- 동일한 시나리오 무한 반복
- 목적 없는 장시간 로그 수집

## 7. 에뮬레이터 테스트 지침

에뮬레이터는 코드 분석과 빌드만으로 확인할 수 없는 경우에만 사용한다.

에뮬레이터 테스트가 필요하다면 다음 절차를 따른다.

1. 기존에 실행 중인 에뮬레이터가 있는지 먼저 확인한다.
2. 가능하면 기존 에뮬레이터를 재사용한다.
3. 필요한 앱만 설치하거나 실행한다.
4. 이번 문제를 확인하는 최소 시나리오만 테스트한다.
5. 테스트 전에 필요한 로그 필터를 설정한다.
6. 관련 없는 전체 Logcat을 장시간 읽지 않는다.
7. 문제가 재현되면 필요한 로그만 확보하고 원인을 분석한다.
8. 코드를 수정한 뒤 동일한 검증을 1회 수행한다.
9. 테스트가 성공하면 반복 실행하지 않는다.

에뮬레이터 테스트가 불가능하거나 사람의 시각적 판단이 필요한 경우에는 억지로 반복하지 말고, 사용자가 직접 확인할 수 있는 테스트 절차를 작성한다.

두 앱 연동 테스트가 필요한 경우:

- `app-manager`와 `app-display`를 구분해서 실행한다.
- 각 앱이 사용하는 주소와 포트를 확인한다.
- Android Emulator에서 개발 PC로 접속할 때 필요한 주소 설정을 확인한다.
- 연결 성공 여부를 로그 또는 명확한 상태값으로 검증한다.
- 버튼을 눌렀다는 사실만으로 성공으로 판단하지 않는다.
- 요청 전송, 요청 수신, 상태 변경, UI 반영까지 단계별로 확인한다.

## 8. 반복 수정 제한

수정과 테스트가 실패하더라도 무작정 다른 코드를 계속 변경하지 않는다.

동일한 문제가 두 번 이상 반복되면 다음을 먼저 수행한다.

1. 현재 가설이 틀렸는지 검토한다.
2. 실제 실행 로그를 다시 확인한다.
3. 호출되지 않는 코드나 잘못된 실행 환경을 확인한다.
4. 네트워크 주소, 포트, 앱 설치 버전, 프로세스 상태를 확인한다.
5. 새로운 수정 전에 원인을 다시 설명한다.

한 번에 여러 해결책을 동시에 적용하지 않는다. 원인을 확인할 수 있도록 하나의 변경 단위로 진행한다.

## 9. 금지 사항

다음 작업은 사용자가 명시적으로 요청하지 않는 한 수행하지 않는다.

- 프로젝트 전체 리팩터링
- UI 전면 변경
- Gradle 또는 라이브러리 대규모 업그레이드
- 아키텍처 교체
- 파일 및 클래스 대량 이동
- 기존 API 규격 변경
- 데이터 모델 전체 변경
- 테스트를 통과시키기 위한 기능 제거
- 오류를 숨기기 위한 예외 무시
- 임의의 지연 시간 추가
- 원인을 확인하지 않은 재시도 코드 추가
- 사용자가 작성한 미커밋 변경 사항 삭제
- 테스트 성공 전 완료 처리

## 10. 작업 완료 기준

다음 조건을 충족해야 작업을 완료한 것으로 판단한다.

- 문제의 실제 원인을 확인했다.
- 최소 범위의 코드만 수정했다.
- 수정된 모듈이 빌드된다.
- 가능한 범위에서 관련 테스트를 수행했다.
- 기존 주요 기능에 영향을 주지 않았는지 검토했다.
- 사용자가 직접 확인할 테스트 방법을 작성했다.
- 확인하지 못한 사항을 성공했다고 표현하지 않았다.

## 11. 최종 보고 형식

작업이 끝나면 아래 형식으로만 간결하게 보고한다.

### 1. 원인

문제가 발생한 실제 원인을 설명한다.

### 2. 수정 파일

수정한 파일 경로를 모두 작성한다.

### 3. 변경 내용

각 파일에서 무엇을 왜 변경했는지 설명한다.

### 4. 테스트 결과

실제로 수행한 명령과 결과를 작성한다.

예:

- `:app-manager:compileDebugKotlin` — 성공
- `:app-display:assembleDebug` — 성공
- 에뮬레이터 연동 테스트 — 성공 또는 미수행

수행하지 않은 테스트는 수행했다고 작성하지 않는다.

### 5. 사용자 확인 방법

사용자가 Android Studio 또는 실제 기기에서 확인할 수 있도록 번호 순서로 작성한다.

### 6. 남은 위험 요소

아직 확인하지 못한 사항이나 잠재적인 문제가 있다면 작성한다. 없다면 `확인된 위험 요소 없음`이라고 작성한다.

### 7. 변경 요약

마지막에 다음 형식으로 짧게 정리한다.

```text
작업 목표:
수정 파일:
빌드 결과:
에뮬레이터 테스트:
커밋 생성 여부:
```
