# Commit027.1 적용

프로젝트 루트에 `app-manager` 폴더를 덮어씁니다.

추가 기능:
- 웹 타이머 종료 시 방 카드 빨간색 표시
- 웹 알람음 반복 재생
- Windows/브라우저 알림
- 브라우저 탭 제목에 종료 방 표시
- 웹 화면에서 알람 확인/정지
- 자동 정지 시간 설정: 5/10/15/20/30/45/60초 또는 사용 안 함
- 설정은 브라우저 localStorage에 저장

브라우저 정책상 알람음과 시스템 알림은 첫 접속 후 `웹 알림·소리 사용` 버튼을 한 번 눌러야 합니다.
