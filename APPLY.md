# Commit016 TCP 적용

1. ZIP 안의 파일을 프로젝트 루트에 같은 경로로 덮어씁니다.
2. `DELETE_OLD_UDP_FILES.txt`에 적힌 UDP 파일 3개를 삭제합니다.
3. Android Studio에서 Gradle Sync → Clean Project → Rebuild Project를 실행합니다.
4. 직원용 앱을 먼저 실행합니다.
5. 직원용 홈 화면에 표시되는 `손님용 연결 주소`를 확인합니다.
6. 손님용 앱의 IP 입력란에 해당 주소를 입력하고 `연결`을 누릅니다.

## 테스트 주소

- 같은 에뮬레이터 안에서 두 앱을 테스트할 때: `127.0.0.1`
- 실제 기기 두 대에서 테스트할 때: 직원용 앱에 표시된 `192.168.x.x` 주소

TCP 포트는 45991입니다.
