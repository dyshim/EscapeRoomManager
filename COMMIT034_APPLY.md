# Commit034 적용 안내

## 변경 내용
- WebSocket/TCP 재접속 감지는 백그라운드에서 계속 수행합니다.
- 짧은 순간의 연결 끊김은 UI에 표시하지 않습니다.
- 6초 이상 실제 연결이 복구되지 않을 때만 `연결 끊김`으로 변경합니다.
- 재연결에 성공하면 `실시간 연결됨` 또는 `직원용 앱 연결됨`으로 돌아옵니다.
- 웹과 손님용 앱의 연결 표시를 색상 점 대신 Wi-Fi 아이콘으로 변경했습니다.
- 연결됨: 초록색 Wi-Fi 아이콘
- 연결 끊김: 빨간색 Wi-Fi 아이콘 + 사선
- 연결 확인 중: 노란색 Wi-Fi 아이콘(웹 최초 접속 시에만)
- 손님용 앱은 네트워크 수신 시각을 매초 검사해 UI를 바꾸지 않고, 안정화된 연결 상태만 사용합니다.

## 적용
압축 내용을 프로젝트 루트에 같은 경로로 덮어씁니다.

1. Sync Project with Gradle Files
2. Clean Project
3. Rebuild Project
4. 직원용·손님용 앱 재설치
5. 웹 브라우저의 기존 탭을 닫고 다시 접속

## 확인 방법
1. 정상 연결 중에는 상태 문구가 계속 `실시간 연결됨`으로 고정되는지 확인합니다.
2. Wi-Fi를 1~3초 정도 끊었다가 복구했을 때 문구가 깜빡이지 않는지 확인합니다.
3. Wi-Fi를 6초 이상 끊으면 빨간 Wi-Fi 아이콘과 `연결 끊김`이 표시되는지 확인합니다.
4. Wi-Fi 복구 후 초록 Wi-Fi 아이콘으로 돌아오는지 확인합니다.
